'use strict';
const $ = (selector) => document.querySelector(selector);
const esc = (value) => String(value ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const icons = {
 door:'<rect x="4" y="3" width="16" height="18" rx="2"/><path d="M12 3v18M9 11v3m6-3v3M2 21h20"/>',
 acv:'<path d="M12 2v20M3.3 7l17.4 10M3.3 17L20.7 7M9 4l3 3 3-3M9 20l3-3 3 3M4 10l4-1-1-4M20 14l-4 1 1 4M4 14l4 1-1 4M20 10l-4-1 1-4"/>',
 rail:'<path d="M7 3L4 22M17 3l3 19M6 7h12M5 12h14M4 17h16M3 22h18"/>',
 shm:'<path d="M3 18V6m18 12V6M3 12h4l3-7 4 14 3-7h4M3 22h18"/>'
};
const systems = {
 door:{name:'Doors',short:'Door',subtitle:'Find doors that may be sticking',extension:'.csv',title:'Find door movements that need inspection.',description:'The app finds each opening or closing movement and checks for unusual resistance that could indicate a sticking door.',output:'Start and end times, with a result for each movement',focus:'Movements marked “Abnormal resistance”',metric:'IoU-weighted F1 · checks movement timing and fault labels',action:'Review flagged movements, then check the door tracks, seals and motor.',hint:'Add one CSV file containing a continuous door recording.',format:'Use one CSV file with the original column headings and all 17 columns, including time, motor readings and door position.',method:'Finds individual door movements and compares their motor readings with learned patterns.',limitation:'The app may miss a movement or mark its start or end incorrectly. “Normal” means no unusual resistance was detected; it does not confirm the door is fault-free.'},
 acv:{name:'Air conditioning',short:'ACV',subtitle:'Find the car most likely to have a leak',extension:'.xlsx',title:'See which car to inspect first.',description:'The app compares air-conditioning readings across the train and lists the cars from most to least likely to have a refrigerant leak.',output:'An ordered list of cars for each recording',focus:'The first car in each list',metric:'Rank-decay score · rewards placing the faulty car near the top',action:'Start with the first car in the list, then check the others in order.',hint:'Add Excel files (.xlsx). Each file must include readings for every car.',format:'Keep the original Excel column headings, such as “Car 03 - ACV Running Mode”, and the readings for every car. Different files may contain different measurements.',method:'Compares each car’s temperature with the other cars and uses pressure readings when available.',limitation:'The model was developed using six known fault cases. The order helps you decide where to inspect first; it is not a confirmed leak diagnosis or a percentage chance of a leak.'},
 rail:{name:'Rail condition',short:'Rail',subtitle:'Find uneven wear on either rail',extension:'.csv',title:'Find which rail side needs inspection.',description:'The app checks vibration readings for corrugation: a repeated pattern of uneven rail wear. Results show Normal, Side I or Side II.',output:'A condition result for each recording',focus:'Results marked Side I or Side II',metric:'Macro F1 · gives equal weight to each of the three results',action:'Match flagged files to their track locations, then inspect the indicated rail side.',hint:'Add one or more CSV files, each with a one-second vibration recording.',format:'Use CSV files with the original headings and all 129 columns: one speed signal plus 128 vibration and shock measurements. Each recording covers one second at 10,000 readings per second.',method:'Adjusts vibration measurements for speed, then checks each rail side for learned wear patterns.',limitation:'The model was trained with relatively few faulty examples. A Normal result does not rule out every rail defect. Use your recording records to find the track location.'},
 shm:{name:'Structural health',short:'SHM',subtitle:'Estimate damage from repeated stress',extension:'.csv',title:'Compare damage caused by repeated stress.',description:'The app estimates fatigue damage from stress readings. Larger values indicate more estimated damage for the recorded loading.',output:'One fatigue damage estimate for each file',focus:'The highest estimates, for comparable recordings',metric:'1 − MAPE, minimum 0 · higher scores mean lower percentage error',action:'Review the highest estimates first. Check that recordings cover comparable parts and operating periods.',hint:'Add stress recordings as CSV files without column headings.',format:'Use CSV files with no heading row. The first column must contain numeric stress readings. The app returns one damage estimate per file.',method:'Counts stress cycles and estimates fatigue damage, with a correction learned from training examples.',limitation:'This estimate does not tell you how long a part will last. Do not add values from unrelated parts or recordings.'}
};
const state = {system:'rail',files:[],models:{},runs:[],current:null,busy:false,view:'workspace'};
let toastTimer;
function toast(message){$('#toast').textContent=message;$('#toast').hidden=false;clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('#toast').hidden=true,4200);}
function showError(message){$('#error').textContent=message;$('#error').hidden=!message;}
async function api(url, options){const response=await fetch(url,options);const data=await response.json();if(!response.ok)throw new Error(data.error||'The request could not be completed.');return data;}
function setView(view){state.view=view;window.scrollTo({top:0,behavior:'instant'});document.querySelectorAll('.view').forEach(el=>el.hidden=el.id!==`${view}-view`);document.querySelectorAll('.nav').forEach(el=>el.classList.toggle('active',el.dataset.view===view));$('#page-name').textContent={workspace:'Check train condition',history:'Previous results',models:'Model setup',guide:'How to use this app'}[view];if(view==='history')renderHistory();if(view==='models')renderModels();}
function selectSystem(key){if(state.busy)return;state.system=key;state.files=[];$('#file-input').value='';state.current=null;$('#results').hidden=true;showError('');renderWorkspace();}
function renderWorkspace(){const s=systems[state.system];$('#system-cards').innerHTML=Object.entries(systems).map(([key,item])=>`<button class="system-card ${key===state.system?'selected':''}" data-system="${key}" aria-pressed="${key===state.system}" ${state.busy?'disabled':''}><span class="system-icon"><svg viewBox="0 0 24 24" aria-hidden="true">${icons[key]}</svg></span><span><strong>${item.name}</strong><small>${item.subtitle}</small></span>${key===state.system?'<i class="selected-dot"></i>':''}</button>`).join('');$('#system-cards').querySelectorAll('button').forEach(b=>b.onclick=()=>selectSystem(b.dataset.system));$('#input-type').textContent=s.extension.slice(1).toUpperCase();$('#input-hint').textContent=s.hint;$('#file-input').accept=s.extension;$('#file-input').multiple=state.system!=='door';$('#file-input').disabled=state.busy;$('#upload-limit').textContent=`${s.extension.slice(1).toUpperCase()} · ${state.system==='door'?'one continuous recording':'up to 100 files'} · 120 MB total`;$('#context-title').textContent=s.title;$('#context-description').textContent=s.description;$('#asset-tag').textContent=s.short.toUpperCase();$('#output-summary').textContent=s.output;$('#metric-summary').textContent=s.focus;$('#action-summary').textContent=s.action;renderFiles();renderNotice();}
function renderNotice(){const model=state.models[state.system];const notice=$('#model-notice');notice.className=`notice ${model?.ready?'ready':''}`;notice.textContent=model ? (model.ready?'✓ Model file checked. Add your files, then select Check these files.':'This check is unavailable until its model is installed. Open Model setup for help, or view example results below.'):'Checking whether the model is installed…';$('#analyze').disabled=state.busy||!state.files.length||!model?.ready;$('#analyze').innerHTML=state.busy?'Checking your files…':'Check these files <span>↗</span>';$('#preview').disabled=state.busy;$('#progress').hidden=!state.busy;}
async function refreshStatus(){try{state.models=(await api('/api/status')).models;renderNotice();if(state.view==='models')renderModels();}catch(error){$('#model-notice').textContent='The app has lost its connection. Restart the app and refresh this page.';showError(error.message);}}
function renderFiles(){$('#file-list').innerHTML=state.files.map((file,i)=>`<div class="file-item"><span title="${esc(file.name)}">▤ ${esc(file.name)}</span><small>${file.size<1048576?(file.size/1024).toFixed(0)+' KB':(file.size/1048576).toFixed(1)+' MB'}</small><button class="icon-button" aria-label="Remove ${esc(file.name)}" data-index="${i}" ${state.busy?'disabled':''}>×</button></div>`).join('');$('#file-list').querySelectorAll('button').forEach(b=>b.onclick=()=>{state.files.splice(Number(b.dataset.index),1);renderFiles();renderNotice();});}
function addFiles(files){if(state.busy)return;showError('');const next=[...state.files,...files];const s=systems[state.system];if(next.some(f=>!f.name.endsWith(s.extension))){showError(`Choose ${s.extension} files for ${s.name}. Check that you chose the right train system.`);return;}if(next.some(f=>!f.size)){showError('An uploaded file is empty. Choose a recording containing sensor data.');return;}if(new Set(next.map(f=>f.name.toLowerCase())).size!==next.length){showError('A filename is repeated. Remove the duplicate before continuing.');return;}if(next.length>(state.system==='door'?1:100)){showError(state.system==='door'?'For Doors, choose one continuous recording. Remove the selected file before choosing another.':'Choose up to 100 files at a time.');return;}if(next.reduce((a,f)=>a+f.size,0)>120*1048576){showError('These files total more than 120 MB. Upload fewer files now and check the rest afterwards.');return;}state.files=next;renderFiles();renderNotice();}
function addRun(run){state.runs.unshift(run);state.runs=state.runs.slice(0,40);state.current=run;$('#history-count').textContent=state.runs.length;$('#export-count').textContent=`${Object.keys(latestLive()).length} / 4`;renderResults(run);}
function latestLive(){const latest={};for(const run of state.runs){if(run.preview)continue;if(!latest[run.system])latest[run.system]={rows:[],names:new Set()};const group=latest[run.system];if(run.system==='door'){if(!group.rows.length)group.rows=run.rows;}else for(const row of run.rows){if(!group.names.has(row.file_id)){group.names.add(row.file_id);group.rows.push(row);}}}return latest;}
async function analyze(){if(state.busy)return;state.busy=true;state.current=null;$('#results').hidden=true;showError('');renderWorkspace();const data=new FormData();data.append('system',state.system);state.files.forEach(file=>data.append('files',file,file.name));try{const run=await api('/api/analyze',{method:'POST',body:data});addRun(run);toast('Analysis complete. Your results are ready.');$('#results').scrollIntoView({behavior:'smooth',block:'start'});}catch(error){showError(error.message);}finally{state.busy=false;renderWorkspace();refreshStatus();}}
async function preview(){if(state.busy)return;const key=state.system;showError('');$('#preview').disabled=true;try{const run=await api(`/api/preview?system=${key}`);if(key!==state.system)return;addRun(run);$('#results').scrollIntoView({behavior:'smooth',block:'start'});}catch(error){showError(error.message);}finally{$('#preview').disabled=false;}}
function summary(run){const rows=run.rows,key=run.system;if(key==='rail'||key==='door'){const abnormal=rows.filter(r=>r.prediction!=='Normal').length;return [{label:key==='door'?'Door movements found':'Recordings checked',value:rows.length,detail:key==='door'?'In this recording':'Files in this check'},{label:'Needs inspection',value:abnormal,detail:key==='door'?'Abnormal resistance':'Uneven wear on one rail side'},{label:'Marked Normal',value:rows.length-abnormal,detail:'No target fault detected'}];}if(key==='acv'){const first=rows[0].ranked_cars.split('|');return [{label:'Recordings checked',value:rows.length,detail:'One car list per recording'},{label:'First recording · inspect first',value:`Car ${first[0]}`,detail:'Most likely to have a leak'},{label:'Cars in first recording',value:first.length,detail:'Listed in inspection order'}];}const vals=rows.map(r=>+r.prediction);return [{label:'Recordings checked',value:rows.length,detail:'Files containing stress readings'},{label:'Highest damage estimate',value:Math.max(...vals).toFixed(4),detail:'A damage value with no units'},{label:'Middle damage estimate',value:median(vals).toFixed(4),detail:'Median across these files'}];}
function median(values){const a=[...values].sort((a,b)=>a-b);return (a[Math.floor((a.length-1)/2)]+a[Math.floor(a.length/2)])/2;}
function insight(run){const rows=run.rows;if(run.system==='rail'){const n=rows.filter(r=>r.prediction!=='Normal').length;const side1=rows.filter(r=>r.prediction==='Side I').length;return n?`${n} recording${n===1?'':'s'} need review: ${side1} on Side I and ${n-side1} on Side II. Match each recording to its track location, then inspect the indicated side for uneven wear.`:'No uneven rail wear was detected in these recordings. Continue routine inspections; these results do not rule out all rail defects.';}if(run.system==='door'){const n=rows.filter(r=>r.prediction!=='Normal').length;return n?`${n} door movements show unusual resistance. Use their start and end times to find them in the recording, then inspect the tracks, seals and motor.`:'No unusual resistance was detected. Check that the app found every opening and closing movement in the original recording.';}if(run.system==='acv')return 'For each recording, inspect the first car in the list, then work through the others. Check cooling performance and the refrigerant circuit. The list shows inspection order, not the probability of a leak.';const high=rows.reduce((a,b)=>+a.prediction>+b.prediction?a:b);return `${high.file_id} has the highest estimated damage (${(+high.prediction).toFixed(4)}). Compare recordings from similar parts over similar operating periods. This estimate does not tell you how long the part will last.`;}
function chart(run){const rows=run.rows;if(run.system==='rail'){const colors=['#91a87c','#ce8d66','#496d5b'];const labels=['Normal','Side I','Side II'];return `<div class="chart-title"><strong>Results by rail condition</strong><div class="legend"><span><i></i>Normal</span><span><i class="orange"></i>Side I</span><span><i class="dark"></i>Side II</span></div></div><div class="distribution" role="img" aria-label="${labels.map(l=>`${l}: ${rows.filter(r=>r.prediction===l).length}`).join(', ')}">${labels.map((l,i)=>{const n=rows.filter(r=>r.prediction===l).length;return n?`<div style="width:${n/rows.length*100}%;background:${colors[i]}" title="${l}: ${n}">${n}</div>`:'';}).join('')}</div><p class="data-note">Each file has one result. Side I uses sensors at wheel positions 1, 3, 5 and 7; Side II uses positions 2, 4, 6 and 8.</p>`;}if(run.system==='door')return `<div class="chart-title"><strong>Door movements in recording order</strong><div class="legend"><span><i></i>Normal</span><span><i class="orange"></i>Abnormal resistance</span></div></div><div class="segments">${rows.map((r,i)=>`<button class="segment ${r.prediction!=='Normal'?'flag':''}" data-cycle="${i}" aria-label="Movement ${i+1}: ${esc(r.prediction)}; ${esc(r.start_time)} to ${esc(r.end_time)}" title="${esc(r.start_time)} → ${esc(r.end_time)}">${String(i+1).padStart(2,'0')}</button>`).join('')}</div><p id="cycle-detail" class="data-note">Select a numbered movement to see when it started and ended. The boxes show order, not how long each movement lasted.</p>`;if(run.system==='acv')return `<div class="chart-title"><strong>Car inspection order</strong><label class="filters">Recording <select id="acv-case" aria-label="Choose an air-conditioning recording">${rows.map((r,i)=>`<option value="${i}">${esc(r.file_id)}</option>`).join('')}</select></label></div><div id="ranking" class="ranking">${ranking(rows[0])}</div><p class="data-note">Start with the car on the left: it is most likely to have a leak. Car numbers match your original file.</p>`;const sorted=[...rows].sort((a,b)=>+b.prediction-+a.prediction).slice(0,24);const max=Math.max(...sorted.map(r=>+r.prediction),.000001);return `<div class="chart-title"><strong>Estimated fatigue damage · highest first</strong><span class="muted">Damage value · no units</span></div><div class="bars" role="img" aria-label="Highest damage estimate recordings; exact values are available in the table">${sorted.map((r,i)=>`<div class="bar-column" style="height:${(+r.prediction/max)*100}%" title="${esc(r.file_id)}: ${(+r.prediction).toPrecision(6)}"><span>${i+1}</span></div>`).join('')}</div><p class="data-note">${sorted.length} highest estimates shown. Bars are ordered by estimated damage, not recording time. They do not show how soon a part might fail.</p>`;}
function ranking(row){return row.ranked_cars.split('|').map((c,i)=>`<div class="rank-car"><small>RANK ${i+1}</small><strong>${esc(c)}</strong><span>${i===0?'INSPECT FIRST':'CAR'}</span></div>`).join('');}
function renderResults(run){const s=systems[run.system];$('#results').hidden=false;$('#results').innerHTML=`<div class="results-header"><div><span class="result-badge ${run.preview?'':'live'}">${run.preview?'EXAMPLE RESULTS · YOUR FILES WERE NOT CHECKED':'RESULTS FROM YOUR UPLOADED FILES'}</span><h2>${s.name} results</h2></div><div class="results-actions"><a class="button secondary" href="/api/runs/${run.id}/report">↓ Technical record (JSON)</a><a class="button primary" href="/api/runs/${run.id}/csv">↓ ${run.preview?'Example results (CSV)':'Download results (CSV)'}</a></div></div><p class="result-meta">${run.preview?'Previously saved predictions. The original files and model version have not been verified.':`${esc(run.model.run_id||'Active model')} · ${run.elapsed.toFixed(2)} seconds · ${run.files.length} input file(s)`}</p><div class="stat-grid">${summary(run).map(s=>`<div class="stat"><small>${s.label}</small><strong>${esc(s.value)}</strong><span>${s.detail}</span></div>`).join('')}</div><div class="insight"><span>↗</span><div><strong>What to check next</strong><p>${esc(insight(run))}</p></div></div>${chart(run)}<div class="table-tools"><h3>Results for each item <span class="muted">/ ${run.rows.length}</span></h3><div class="filters"><input id="result-search" type="search" placeholder="Search results…" aria-label="Search results">${['rail','door'].includes(run.system)?'<select id="result-filter" aria-label="Filter results"><option value="all">All results</option><option value="flagged">Needs inspection</option><option value="normal">Marked Normal</option></select>':''}</div></div><div class="table-wrap"><table><thead id="result-head"></thead><tbody id="result-body"></tbody></table></div><p id="row-count" class="data-note"></p><p class="data-note">${esc(s.limitation)}</p>`;$('#result-search').oninput=()=>renderTable(run);if($('#result-filter'))$('#result-filter').onchange=()=>renderTable(run);if($('#acv-case'))$('#acv-case').onchange=e=>$('#ranking').innerHTML=ranking(run.rows[Number(e.target.value)]);$('#results').querySelectorAll('[data-cycle]').forEach(b=>b.onclick=()=>{const i=Number(b.dataset.cycle),r=run.rows[i];$('#cycle-detail').textContent=`Movement ${i+1} · ${r.prediction} · ${r.start_time} → ${r.end_time}`;});renderTable(run);}
function renderTable(run){const search=$('#result-search').value.toLowerCase(),filter=$('#result-filter')?.value||'all';const rows=run.rows.map((r,i)=>({...r,index:i+1})).filter(r=>Object.values(r).some(v=>String(v).toLowerCase().includes(search))&&(filter==='all'||(filter==='flagged'?r.prediction!=='Normal':r.prediction==='Normal')));const headers=run.system==='door'?['Movement','Start time','End time','Result']:run.system==='acv'?['Source file','Cars · inspect from left to right']:['Source file',run.system==='shm'?'Estimated fatigue damage':'Result'];$('#result-head').innerHTML=`<tr>${headers.map(h=>`<th scope="col">${h}</th>`).join('')}</tr>`;$('#result-body').innerHTML=rows.slice(0,500).map(r=>{let cells;if(run.system==='door')cells=[String(r.index).padStart(2,'0'),esc(r.start_time),esc(r.end_time),pill(r.prediction)];else if(run.system==='acv')cells=[esc(r.file_id),esc(r.ranked_cars.split('|').join(' → '))];else cells=[esc(r.file_id),run.system==='shm'?(+r.prediction).toPrecision(7):pill(r.prediction)];return `<tr>${cells.map(c=>`<td>${c}</td>`).join('')}</tr>`;}).join('')||`<tr><td colspan="${headers.length}">No results match your search or filter.</td></tr>`;$('#row-count').textContent=`Showing ${Math.min(rows.length,500)} of ${rows.length} matching results. Downloaded files include all results, with the full numeric precision returned by the model.`;}
function pill(value){return `<span class="pill ${value!=='Normal'?'flag':''}">${esc(value)}</span>`;}
function renderHistory(){$('#history-content').innerHTML=state.runs.length?state.runs.map(r=>`<article class="history-item"><span class="result-badge ${r.preview?'':'live'}">${r.preview?'EXAMPLE':'YOUR FILES'}</span><div><h3>${systems[r.system].name}</h3><p>${new Date(r.created).toLocaleString()} · ${r.rows.length} results${r.preview?'':` · ${r.elapsed.toFixed(2)}s`}</p></div><button class="button secondary" data-run="${r.id}">View results →</button></article>`).join(''):'<div class="panel empty"><h2>No previous results yet.</h2><p>Check your files or view example results to see them listed here.</p><button id="history-start" class="button primary">Check train condition →</button></div>';$('#history-content').querySelectorAll('[data-run]').forEach(b=>b.onclick=()=>{const r=state.runs.find(r=>r.id===b.dataset.run);selectSystem(r.system);state.current=r;setView('workspace');renderResults(r);$('#results').scrollIntoView({behavior:'smooth'});});if($('#history-start'))$('#history-start').onclick=()=>setView('workspace');}
function renderModels(){$('#model-content').innerHTML=Object.entries(systems).map(([key,s])=>{const m=state.models[key];return `<article class="panel model-card"><h2>${s.name}</h2><div class="status-line">${m?.ready?'✓ Model file checked':'○ Model not ready'} · ${esc(m?.message||'Checking status…')}</div><dl><dt>How this check works</dt><dd>${esc(m?.details?.method||s.method)}</dd></dl>${m?.details?`<p><strong>${esc(m.details.name)}</strong></p>`:''}<details class="technical-details"><summary>Show setup and scoring details</summary><dl><dt>Model file location</dt><dd><code>${key==='rail'?'Rail Corrugation':s.short}/model/${esc(m?.artifact||'model file')}</code></dd><dt>Model version</dt><dd>${esc(m?.run_id||'Not available')}</dd><dt>File fingerprint (SHA-256)</dt><dd><code>${esc(m?.sha256||'Available after the model file is installed')}</code></dd>${m?.details?`<dt>Selected model settings</dt><dd>${esc(m.details.settings)}</dd><dt>Reported submission result</dt><dd>${esc(m.details.score)}</dd>`:''}<dt>How researchers score this model</dt><dd>${s.metric}</dd></dl></details><p class="muted">${s.limitation}</p></article>`;}).join('');}
function openExport(){const latest=latestLive();$('#export-checklist').innerHTML=Object.entries(systems).map(([key,s])=>`<div class="check-row"><span>${latest[key]?'✓':'○'}</span><strong>${s.name}</strong><small>${latest[key]?`${latest[key].rows.length} results from your uploaded files`:'No uploaded files checked yet'}</small></div>`).join('');$('#download-zip').disabled=!Object.keys(latest).length;$('#export-error').hidden=true;$('#export-dialog').showModal();}
async function downloadZip(){const button=$('#download-zip');button.disabled=true;try{const response=await fetch('/api/export',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({ids:state.runs.filter(r=>!r.preview).map(r=>r.id)})});if(!response.ok)throw new Error((await response.json()).error);const url=URL.createObjectURL(await response.blob());const a=document.createElement('a');a.href=url;a.download='predictions.zip';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);$('#export-dialog').close();toast('Your submission ZIP file has been downloaded.');}catch(error){$('#export-error').textContent=error.message;$('#export-error').hidden=false;}finally{button.disabled=false;}}
document.querySelectorAll('.nav').forEach(button=>button.onclick=()=>setView(button.dataset.view));$('#file-input').onchange=e=>{addFiles([...e.target.files]);e.target.value='';};const dropzone=$('#dropzone');['dragenter','dragover'].forEach(name=>dropzone.addEventListener(name,e=>{e.preventDefault();dropzone.classList.add('dragover');}));['dragleave','drop'].forEach(name=>dropzone.addEventListener(name,e=>{e.preventDefault();dropzone.classList.remove('dragover');}));dropzone.addEventListener('drop',e=>addFiles([...e.dataTransfer.files]));$('#analyze').onclick=analyze;$('#preview').onclick=preview;$('#refresh-models').onclick=refreshStatus;$('#export-open').onclick=openExport;$('#close-export').onclick=()=>$('#export-dialog').close();$('#download-zip').onclick=downloadZip;$('.train-cars').innerHTML=Array.from({length:8},(_,i)=>`<div class="car"><i></i><i></i><i></i><b>${String(i+1).padStart(2,'0')}</b></div>`).join('');$('#format-guide').innerHTML=Object.values(systems).map(s=>`<h3>${s.name}</h3><p>${s.format}</p>`).join('');renderWorkspace();refreshStatus();

async function restoreHistory(){try{state.runs=(await api('/api/history')).runs;$('#history-count').textContent=state.runs.length;$('#export-count').textContent=`${Object.keys(latestLive()).length} / 4`;if(state.view==='history')renderHistory();}catch(error){toast('History could not be restored: '+error.message);}}
restoreHistory();
