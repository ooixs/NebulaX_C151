# Team C151 — PS3 Train Condition Monitoring: Write-up

This single write-up covers all subsystems attempted (per the spec, one copy at the top level of
`Optional_Items/` is acceptable when it spans multiple subsystems).

## Door

_Approach, segmentation method, features, model, train/validation split, results, assumptions._

## ACV

_Approach, per-car features, ranking method, leave-one-case-out validation, results, assumptions._

## Rail Corrugation

_Approach, speed normalisation, per-side features, model, CV results (macro F1), assumptions._

## SHM

_Approach, rainflow + Miner's-rule recovery, fitted S-N parameters, CV results (1 − MAPE), assumptions._
